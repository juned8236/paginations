settings.py

installed app[
    'rest_framework',
]

sudo pip3 install djangorestframework

REST_FRAMEWORK={
'DEFAULT_PAGINATION_CLASS':'rest_framework.pagination.PageNumberPagination',
'PAGE_SIZE':10,
}

==============================
run populate.py separately
pip3 install faker
